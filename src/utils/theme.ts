import { createTheme } from "@mui/material";

export default createTheme({
  typography: {
    fontFamily: ["ComicNeue", "cursive"].join(","),
    h4: {
      fontSize: "1.8em",
    },
  },
});

export const xlHorizontalPadding = "200px";
export const mdHorizontalPadding = "140px";
